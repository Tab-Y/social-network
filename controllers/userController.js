const { ObjectId } = require('mongoose').Types;
const {  } = require('../models');      // models for required 

module.exports = { 
    // all gets, puts, deletes for users

    // username
    // email
    // thoughts
    // friends
    // virtual called friendCount that retrieves frends length
 };